const sum = (a, b) => a + b;

test('Testando se sum(1, 2) retorna 3', () => {
    expect(sum(1, 2)).toEqual(3);
})

/* const uppercase = (str, callback) => {
    callback(str.toUpperCase()); */
  };

/* console.log(uppercase('olá', result)); */

//para refazer este exercício, lembre-se de inserir um "test" ou "spec" no nome do arquivo.
